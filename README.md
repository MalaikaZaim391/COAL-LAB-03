Task 1: Convert the following high-level instruction into Assembly Language:
1. ebx = { (a+b) – (a-b) + c } +d
2. a= 10h, b=12h, c=30h, d=20h

Task 2: Declare an array of 10 unsigned bytes named bArray and initialize all elements.

Task 3: Declare a string variable containing the word “NUCES” repeated 200 times.

Task 4: Declare an uninitialized array of 30 unsigned doublewords named dArray.

Task 5: Declare an unsigned 16-bit integer variable named that uses three Initializers.

Task 6: FAST NUCES university database system needs to store marks of 5 students in 3 different subjects (DS, COAL, and Database). Declare a two-dimensional array in Assembly language to store this data.

Task 7: Karachi Bakery sales system records the daily sales of 7 days. Declare an array of signed integers to store sales data. Initialize it with sample values.

Task 8: Declare a 32-bit signed integer variable num1 initialized with 1000h. Then declare another 32-bit signed integer num2.

Task 9: Declare an array of 20 unsigned words named uArray. Initialize all elements with the value 5.

Task 10: Declare an uninitialized string variable of 100 characters named msg. Then store the string " COMPUTER ORGANIZATION & ASSEMBLY LANGUAGE LAB" in it as a null- terminated string.
